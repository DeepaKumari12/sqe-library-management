# Test Cases — SQE Library Management System

| ID | Title | Requirement | Preconditions | Steps | Expected | Priority | Type |
|---|---|---|---|---|---|---|---|
| TC-001 | Borrow a book with a valid borrower name | REQ-1 | A `Book` instance exists with `status="available"`, `borrower=None` | 1. Call `book.borrow("Aisha Khan")` | `book.status == "borrowed"` and `book.borrower == "Aisha Khan"`; no exception raised | High | Positive / Functional |
| TC-002 | Reject borrow with empty string borrower name | REQ-2 | A `Book` instance exists with `status="available"` | 1. Call `book.borrow("")` | `ValueError` raised with message "Borrower name cannot be empty."; book state unchanged | High | Negative / Functional |
| TC-003 | Reject borrow with `None` as borrower name | REQ-2 | A `Book` instance exists with `status="available"` | 1. Call `book.borrow(None)` | `ValueError` raised with message "Borrower name cannot be empty."; book state unchanged | High | Negative / Functional |
| TC-004 | Reject borrow with whitespace-only borrower name | REQ-2 | A `Book` instance exists with `status="available"` | 1. Call `book.borrow("   ")` | `ValueError` raised with message "Borrower name cannot be empty."; book state unchanged | Medium | Negative / Functional |
| TC-005 | Prevent double-borrow via normal call | REQ-3 | A `Book` is already borrowed (`status="borrowed"`, `borrower="Sana"`) | 1. Call `book.borrow("Zain")` | `ValueError` raised containing the book's title and "is already borrowed."; original borrower (`"Sana"`) unchanged | High | Negative / Functional |
| TC-006 | Prevent double-borrow bypass via direct attribute manipulation | REQ-3 | A `Book` is available; test manually sets `book.borrower = "Sana"` without going through `borrow()`, leaving `status="available"` | 1. Manually set `book.borrower = "Sana"`<br>2. Call `book.borrow("Zain")` | `ValueError` raised (the `borrower is not None` check catches the bypass even though `status` was never updated) | High | Negative / Security-adjacent |
| TC-007 | Reject duplicate `item_id` on Book creation | REQ-4 | An existing `Book` with `item_id="B-100"` is already in the catalog/registry | 1. Attempt to construct a new `Book` with `item_id="B-100"` | An exception is raised preventing the duplicate `item_id` from being created | High | Negative / Functional |
| TC-008 | Allow unique `item_id` values on Book creation | REQ-4 | Catalog contains a `Book` with `item_id="B-100"` | 1. Construct a new `Book` with `item_id="B-101"` | Book is created successfully with no exception | Medium | Positive / Functional |
| TC-009 | Return a borrowed book | REQ-5 | A `Book` is borrowed (`status="borrowed"`, `borrower="Aisha Khan"`) | 1. Call `book.return_book()` | `book.status == "available"` and `book.borrower is None`; no exception raised | Critical | Positive / Functional (Blocked — method not yet implemented, Issue #17) |
| TC-010 | Reject returning a book that is not currently borrowed | REQ-5 | A `Book` instance exists with `status="available"` | 1. Call `book.return_book()` | An exception (e.g. `ValueError`) is raised indicating the book is not currently borrowed; state unchanged | Medium | Negative / Functional (Blocked — method not yet implemented, Issue #17) |
| TC-011 | Treat borrower names as case-insensitive for duplicate-borrower checks | REQ-6 | A `Book` is borrowed by `"aisha khan"` (lowercase) | 1. Attempt to identify/compare the borrower using `"Aisha Khan"` (different casing) | System recognizes both strings as the same borrower (case-insensitive match); no inconsistent record created | Low | Functional (Won't fix this sprint — Issue #19; expected to currently FAIL) |
| TC-012 | Reject an empty string as a valid book title | REQ-7 | None | 1. Attempt to construct a `Book` with `title=""` | An exception is raised rejecting the empty title | Low | Negative / Functional (Won't fix this sprint — Issue #18; expected to currently FAIL) |

**Negative/error-path test count:** TC-002, TC-003, TC-004, TC-005, TC-006, TC-007, TC-010, TC-012 — 8 of 12 are negative tests (exceeds the minimum of 3 required).